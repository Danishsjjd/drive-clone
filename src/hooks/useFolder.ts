import { useEffect } from "react"

const useFolder = ({
  folderId,
  folderName,
}: {
  folderId: string | null
  folderName: string | null
}) => {
  useEffect(() => {}, [])
  return null
}

export default useFolder
